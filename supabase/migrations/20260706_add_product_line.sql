-- ============================================================
-- CPS — 製造工程に「商品ライン」軸を追加
-- route（枝1/枝2 の並行ブランチ）とは別に、製造工程を商品ライン
-- （ウッドリング / ウッドバングル / クリスタルウッドリング 等）で
-- グループ化するための列。既存データは一切変更しない（追加のみ）。
-- 値は工程編集フォーム（各工程の詳細ページ）から設定する。
-- ============================================================

ALTER TABLE cps_processes
  ADD COLUMN IF NOT EXISTS product_line TEXT;

-- 補足: フローマップの製造レーンは product_line でグループ化される。
-- product_line が未設定の製造工程は従来どおり route（枝）でレーン分けされる。
